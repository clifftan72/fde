# Resend API key — used by the /api/waitlist serverless function (server-side only)
# Add this in Vercel project settings: Settings → Environment Variables
# Never commit the actual value. Rotate immediately if exposed.
RESEND_API_KEY=
